apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/vghule/rsvpapp:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/vghule/rsvpapp:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: rsvpapp
        chartPath: charts/rsvpapp
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/vghule/rsvpapp"
          image.tag: "{{.DIGEST_HEX}}"
